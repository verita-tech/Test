---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart k3s
  ansible.builtin.systemd_service:
    name: k3s
    state: restarted
    daemon_reload: true
